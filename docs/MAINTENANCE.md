# Maintenance checks

Diagnose only. Do not apply, repair, update, or commit during a check. Run each applicable probe. Compare the result with the pass criterion. Report each failure and each skipped check in one line.

## Terminal stack

| ID  | Probe                                                                                                                                         | Pass                                                                                                                 |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| T1  | `ghostty +validate-config`; `ghostty +show-config`                                                                                            | No configuration error. Shell integration keeps `path`                                                               |
| T2  | `(for f in ~/.zshenv ~/.config/zsh/.zshenv ~/.config/zsh/.zprofile ~/.config/zsh/.zshrc ~/.config/zsh/*.zsh; do zsh -n "$f" \|\| exit; done)` | Exit 0                                                                                                               |
| T3  | `whence -p node brew zsh` and `print -l $path` in bare, interactive, login, and `env -i` zsh                                                  | fnm node and Homebrew come before the system copies. User commands come first                                        |
| T4  | `zsh -ic 'print -l $fpath'`; compare `bindkey` before and after a startup change                                                              | No duplicate in fpath. Ctrl+R is Atuin, Ctrl+T is the file widget, Ctrl+F is mdr                                     |
| T5  | In a new shell: `chezmoi re-add ~/.config/zsh/<Tab>`; select, then cancel the line                                                            | The picker includes dotfiles. A selection completes a target path                                                    |
| T6  | Ctrl+T on an empty argument, on `~/.config/zsh/fz`, on a quoted path with spaces, and on a word before another argument; repeat with Escape   | Root and query follow the argument. A selection replaces it. Other text and cancel stay intact                       |
| T7  | Ctrl+T in a temporary Git fixture with hidden, ignored, directory, and binary entries; press `<C-g>`                                          | Default respects ignores. `<C-g>` shows ignored paths. Previews fit the entry type                                   |
| T8  | Compare each `plugins.lock` revision with `git -C "$ZPLUGDIR/<repo-name>" rev-parse HEAD`                                                     | Recorded and installed revisions agree                                                                               |
| T9  | `shellcheck` on changed tmux Bash scripts; parse and load the tmux config on an isolated socket                                               | No new diagnostic. No effect on a live session                                                                       |
| T10 | `tmux list-clients -F '#{client_termname}: #{client_termfeatures}'`; `tmux show -g extended-keys`; read the root bindings                     | Client features and Alt+Shift routing agree with [ARCHI.md](ARCHI.md#terminal-stack)                                 |
| T11 | `M-H/J/K/L` across shell panes and Neovim splits; Ctrl+Shift+W/E/A/S in shell and editor; copy text                                           | The correct consumer gets each key. The clipboard works. Shell Ctrl+Shift+W deletes to line start                    |
| T12 | Switcher: a file deeper than four directories, a text match after line 5,000, a query change, a Files/Grep switch, a bookmark                 | Deep and late matches appear. The query reloads. Files mode restores fuzzy search. The bookmark marker stays visible |
| T13 | `nvim --headless '+checkhealth' +qa`; compare lazy-lock entries with installed plugin heads                                                   | No provider or tool failure. Lock and installed revisions agree                                                      |
| T14 | Save Markdown with two trailing spaces and Lua with trailing spaces; repeat Lua with autoformat off; open a source buffer                     | The Markdown hard break stays. Enabled Lua trim runs. Disabled trim does not. No implicit chezmoi apply              |
| T15 | Repeated `/usr/bin/time -p zsh -ic exit`; `ZPROF=true zsh -ic exit` for attribution                                                           | Medians match the same-machine baseline                                                                              |
| T16 | Shader on and off with equal window size, display, text, focus, and scroll workload                                                           | CPU and GPU or energy recorded separately                                                                            |

Give every tmux probe a unique `-L` or `-S`, including cleanup. `-f /dev/null` selects a config, not a server. `TMUX_TMPDIR` does not override an inherited `$TMUX`. Read the parse output; tmux can exit 0 with an error in it. Use disposable fixtures for write and key-routing checks. A completion test must not run the completed command.

## Agent rig

| ID  | Probe                                                                                                                                                                       | Pass                                                                                                                                                                                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | `~/.bin/chezmoi-claude-doctor.sh`                                                                                                                                           | Exit 0. No `WARN:`, `ORPHAN:`, or `LINT:` line                                                                                                                                                           |
| M2  | `claude plugin list`; compare `gitCommitSha` with `git -C ~/DEV/rd/claude-code-plugins rev-parse HEAD`                                                                      | Expected plugins enabled. First-party revisions agree, or the gap is reported                                                                                                                            |
| M3  | `claude mcp list`                                                                                                                                                           | Configured servers connect. No scope conflict                                                                                                                                                            |
| M4  | `~/.bin/chezmoi-claude-hooks-test.sh --all`                                                                                                                                 | Both suites pass. Report each count                                                                                                                                                                      |
| M5  | `chezmoi diff`                                                                                                                                                              | Empty, or the drifted paths listed without an apply                                                                                                                                                      |
| M6  | `rtk hook check 'grep -r x .'`; `rtk gain`; read `.bash_wrap` in `hook_config.json` and the corruption log                                                                  | Each dispatcher-owned command returns `No rewrite`. Fewer than five corruption entries in seven days                                                                                                     |
| M7  | `command -v actionlint shellcheck hadolint ruff go staticcheck gofmt shfmt mdformat`                                                                                        | All resolve                                                                                                                                                                                              |
| M8  | Inspect `~/.claude/markers`                                                                                                                                                 | Fewer than 400 files. No non-exempt file older than two days                                                                                                                                             |
| M9  | Inspect the Cavemem worker state and M3                                                                                                                                     | `lastError` is null. MCP connects                                                                                                                                                                        |
| M10 | Compare deployed skill directories and symlinks with `~/.local/state/skills/.skill-lock.json`, source `cmd-*`, and plugin registrations                                     | Each skill has an owner. No broken symlink                                                                                                                                                               |
| M11 | Search each `exact_dot_bin/` script and `dot_claude/workflows/*.js` name across the rig and this documentation                                                              | Each has a consumer, or appears in [Operator tools](ARCHI.md#operator-tools) or the workflow registry                                                                                                    |
| M12 | The claim checker below                                                                                                                                                     | Exit 0 and `CLAIMS: CLEAN`                                                                                                                                                                               |
| M13 | `git -C ~/.dotfiles submodule status dot_claude`; `git -C ~/.dotfiles/dot_claude config core.hooksPath`                                                                     | The hook path is `.githooks`. A leading `+` means the worktree and the gitlink disagree: `git add dot_claude` and commit when the worktree is ahead, `git submodule update dot_claude` when it is behind |
| M14 | `git config --get push.recurseSubmodules`; `git config --get submodule.recurse`; `git config --get submodule.dot_claude.update`; `git config --get status.submoduleSummary` | `on-demand`, `false`, `merge`, `true`                                                                                                                                                                    |
| M15 | In a throwaway super/sub pair with both hooks on `core.hooksPath`: commit in the sub, then `commit --amend`, then `commit --amend --date=2001-02-03`                        | The super tip reads `chore(sub): bump to <sha>`. After each amend the super has one bump commit with the new sha, the super author, and the super date                                                   |
| M16 | Feed `.githooks/pre-push` a stdin ref line whose sha records a pushed submodule commit, then one whose gitlink is on no submodule remote                                    | Exit 0, then exit 1 with the submodule, the gitlink, and the push command                                                                                                                                |

```sh
bash "$(jq -r '.plugins["whetstone@raisedadead-plugins"][0].installPath' ~/.claude/plugins/installed_plugins.json)/bin/claim-check" CLAUDE.md docs/README.md docs/ARCHI.md docs/MAINTENANCE.md
```

A dispatcher or hook-rule edit needs M4. A validator or formatter registry edit needs the spec smoke test. A plugin upgrade needs M1 to M3. A shell, Ghostty, tmux, or Neovim change needs the terminal checks. The doctor does not replace them.
