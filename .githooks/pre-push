#!/bin/bash
# Skip reminder when running under `home push` (just sets DOTFILES_PUSH=1)
[ "${DOTFILES_PUSH:-}" = "1" ] && exit 0
printf "\e[0;33mdotfiles ·\e[0m use \e[1mhome push\e[0m to push both repos\n"
