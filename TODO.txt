Future considerations for chezmoi setup
========================================

7. Boolean feature tags in .chezmoi.toml.tmpl
   - Add $personal / $work flags based on hostname detection
   - Use as template guards: {{ if .personal }}...{{ end }}
   - Replaces messy if/else blocks with clean per-machine conditionals
   - Reference: twpayne/dotfiles .chezmoi.toml.tmpl

8. chezmoi hooks for post-apply actions
   - [hooks.apply.post] to auto-reload tmux config after apply
   - Available events: add, apply, read-source-state, git-auto-commit, etc.
   - Each supports .pre and .post
   - Reference: chezmoi.io/reference/configuration-file/hooks/

9. .chezmoiscripts/ directory for organized run scripts
   - Move run_once_setup-private-repo.sh.tmpl into .chezmoiscripts/
   - Organize by OS: .chezmoiscripts/darwin/, .chezmoiscripts/linux/
   - Use run_onchange_before_ prefix for auto-brew-bundle on package list change
   - Reference: twpayne/dotfiles .chezmoiscripts/
